export const getTVShows = (state) => state.shows;

// export const getTVShowById = (state) => {
//   if (!state.shows) return;
//   return state.shows.map((show) => show.show).map((item) => item.id === props.match.params.id);
// };

